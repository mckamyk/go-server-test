import {LitElement, html, css} from 'lit';
import {ScopedElementsMixin as scope} from '@open-wc/scoped-elements';

@scope
export class NewChains extends LitElement {
	render() {
		return html`
		`;
	}

	static styles = css`
	`;

	static get scopedElements() {
		return {
		};
	}
}
